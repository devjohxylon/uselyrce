[build]
builder = "NIXPACKS"

[deploy]
# --import loads Sentry before Express/Discord so request errors are instrumented.
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
